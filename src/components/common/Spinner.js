import "./Spinner.css";
import { CircularProgress } from "@material-ui/core";

const Spinner = () => {
  return (
    <div className="spinnerStyle">
      <CircularProgress />
    </div>
  );
};
export default Spinner;
